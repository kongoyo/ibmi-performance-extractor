# 🔍 全天使用情況分析報告：TB7277206B/U180432/525840

**主機**：clark75 (172.16.12.126) / Library：KTB
**診斷對象**：Job `TB7277206B/U180432/525840`
**分析範圍**：07/14 全天（00:00 ~ 24:00，15 分鐘間隔）

---

## 1. 全天活動總覽

本 Job 是 `TB7277206B` 程式在 07/14 當天**後半段**新建立的作業實例（Job Number `525840`）。掃描全天所有 15 分鐘區間、七大排行榜維度（Transaction Count / Response Time / Total CPU / Interactive CPU / Batch CPU / Disk I/O / Page Faults）後，`525840` 這個 Job Number 僅在 **Transaction Count（交易量）排行榜**中出現，且只出現在以下兩個區間：

| 時間 | Transaction Count | 系統當時段總交易量 | 佔比 | 排行榜名次意義 |
| :--- | ---: | ---: | ---: | :--- |
| 15:15 | **952** 次 | 14,739 次 | 6.5% | 進入 Top 15 |
| 15:30 | **2,028** 次 | 15,824 次（**全天最高峰**） | 12.8% | 進入 Top 15 |

其餘時段（含 15:15 之前、15:45 之後）本 Job Number 皆**未**進入任何維度的 Top 15 排行榜，代表交易量、CPU、回應時間、磁碟 I/O、分頁缺失皆低於當時能上榜的門檻，無異常訊號。

> [!NOTE]
> **Job Number 變更說明**：同一支程式 `TB7277206B` 在同一使用者 `U180432` 下，於當天稍早（10:00、10:30、12:15、12:30、12:45、13:45）曾以另一個 Job Number `207622` 多次出現在交易量排行榜中（單次交易量介於 95～554 次）。IBM i 上 Job Number 改變通常代表**該作業已結束（或逾時斷線）後被重新啟動**的新實例，因此 `207622` 與 `525840` 應視為同一使用者/程式的**前後兩段不同的作業執行期間**，而非同一支持續運行的 Job。本報告聚焦於使用者指定的 `525840` 這一段執行期間。

---

## 2. 15:15 ~ 15:30 尖峰時段診斷

### 系統層次時序 (System Level Context)
15:30 這個區間是**整個 07/14 全天系統交易量的最高峰**（15,824 次），`525840` 恰好在此區間貢獻了 2,028 次交易（約每秒 2.25 筆），是全天系統尖峰的其中一個貢獻來源，但並非唯一或主要成因（佔比僅 12.8%，其餘 87% 來自其他作業）。15:15 區間交易量已開始爬升（952 次），顯示該 Job 在 15:15~15:30 間有明顯的交易量增長趨勢。

### Job 負載排行與資源消耗 (Job Load & Resource Consumption)
| 維度 | 15:15 | 15:30 |
| :--- | :--- | :--- |
| Transaction Count | 952 次（Top 15） | 2,028 次（Top 15） |
| Response Time | 未上榜（低） | 未上榜（低） |
| CPU 消耗 (Total/Interactive/Batch) | 未上榜（低） | 未上榜（低） |
| 磁碟 I/O (Total I/O) | 未上榜（低） | 未上榜（低） |
| 分頁缺失 (Page Faults) | 未上榜（低） | 未上榜（低） |

> [!TIP]
> **高效能微型交易 (Healthy Micro-Transactions)**
> 該 Job 在尖峰時段產生高達兩千餘次交易，但 CPU、Disk I/O、Page Faults 等硬體資源消耗全數低於系統警戒線（皆未上榜），屬於寫得非常輕量、高效率的交易型作業。

### OS 層次診斷（推論）
*基於離線 JSON 快取，未能直接連線 `QAPMJOBOS`，依數據特徵推論：*
高交易量但不吃 CPU、不吃 I/O，常見情境：
1. **記憶體快取讀取**：讀取已完全載入 Main Storage 的小型字典/參照檔，未產生實體 Disk Read。
2. **心跳偵測 / 輪詢**：可能是負責狀態更新或 API 輪詢的高頻小交易。
3. **無鎖交易**：未發生明顯的 Record Lock Wait。

### Pool 聚合分析與分頁缺失
全天未見本 Job 產生值得注意的分頁缺失，代表 Memory Pool 提供了充足的運行空間，記憶體配置健康。

---

## 3. 早段活動參考（Job Number 207622，同程式/同使用者）

| 時間 | Transaction Count |
| :--- | ---: |
| 10:00 | 554 |
| 10:30 | 104 |
| 12:15 | 95 |
| 12:30 | 441 |
| 12:45 | 170 |
| 13:45 | 255 |

早段各區間交易量規模（最高 554 次）明顯低於 15:15~15:30 尖峰（952、2,028 次），且同樣全程未進入 CPU / Response / Disk / Page Fault 排行榜，資源消耗特徵與後段一致，皆為輕量交易。

---

## 🎯 診斷結論與建議

**根因 (Root Cause)**：
**無異常。** `TB7277206B/U180432/525840` 全天僅在 15:15、15:30 兩個區間進入排行榜，且均為 Transaction Count（交易量）維度；即便貢獻了全天系統尖峰交易量的 12.8%，CPU、Disk I/O、Memory 等硬體資源消耗全程維持在低檔，未對系統造成負面負載。

**建議**：
1. **無需修復 (No Action Required)**：目前程式邏輯與資源使用狀況良好。
2. **後續觀察建議**：若日後 15:30 前後系統整體出現效能瓶頸，可將此 Job 排除在懷疑名單之外，優先檢視其餘貢獻 87% 交易量、或在 CPU/Disk/Response 排行榜上榜的作業。
3. **容量規劃參考**：可將此 Job 作為輕量高吞吐交易的效能標竿，證明在避免不必要實體 I/O 的前提下，數千次交易也能對系統毫無負擔。

---
*本報告資料來源：`data/clark75/KTB/perf_0714.json`（07/14 全天離線快取），透過 `npm run rca` 於 15:15、15:30 兩個尖峰時段擷取上下文，並掃描全天所有時段/維度排行榜以確認無其他上榜紀錄。*
